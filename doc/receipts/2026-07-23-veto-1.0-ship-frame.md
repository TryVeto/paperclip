# Veto / Paperclip 1.0 — Ship frame (2026-07-23)

| Field | Value |
|---|---|
| Audience | Sebastian (act-on) |
| Live | veto-mainline → `b314bcf41d46d3207cc609606452b0845a4abf4a` |
| Worktree | `/home/sebastianheyneman_tryveto_com/work/paperclip-v0.07` @ `veto/paperclip-v0.07-overnight-closeout` |
| Hard rule | **Do not** open/unlock Paperclip v0.08 in product/DB. **Do not** fake v0.07 PASS. |
| Artifact URL | https://veto-mainline.tail2fd504.ts.net:10000/veto-1.0-ship-frame/ |

## What “Paperclip / Veto 1.0” means

**Sebastian can run the company on veto-mainline through board + Desktop + Inkbox DM, with Thinking-fast triage alive and version discipline honest — not a version number alone.**

Success look: owner DM round-trips; light wakes finish on the Luna fast path (not Cursor fallthrough); Desktop “View run” works; v0.07 is exact-git closed before the next version opens; natural runs are trustworthy traffic, not probe spam.

## Sequencing

```text
[A] v0.07 honest close          ← GATE (exact-git + live-safe audit PASS)
        │
        ├─ parallel reversible prep (no version unlock):
        │     restore fast-decision source · Desktop View-run · Inkbox receiver/ops
        │     instruction-stack drafts · natural-run canary design
        │
[B] Unlock next version key     ← only after A (rename VET-582 at unlock)
        │
[C] Ship 1.0 product bar under that version
        · Thinking-fast live again
        · Inkbox DM usable (webhook + assignee + Omniagent healthy)
        · Desktop UX fixes cut over
        · Section-2 / natural-run / instruction stack as needed for “usable company”
```

| Rule | Detail |
|---|---|
| Before A | Prep only: source patches, receipts, canary scripts, Inkbox ops that do not claim v0.07 PASS or open the next version. |
| At A | Mac pins fetchable → `audit-v007-live-safe` PASS → then (and only then) unlock. |
| After A | Feature ship + cutover under the new version contract; live still veto-mainline. |
| Never | Unlock v0.08/v1.0 while Mac pins missing; rewrite SPEC pins to substitutes; void capsule to “fix” provenance. |

## Workstream table

| Item | Status now | Owner | Proof of done |
|---|---|---|---|
| **v0.07 Mac exact-git pins** (`9cad4cb…`, `11f92018…`) | **FAIL residual** — objects Mac-only; blob `c9a2782c…` present; live Section-2 corrective on `b314` | **Sebastian** (push) → Cursor (fetch + audit) | Both SHAs `git cat-file -t` on host; `node scripts/audit-v007-live-safe.mjs` → PASS; receipt updated. **Still not a fake PASS today.** |
| **SPEC-1** (package↔journal identity) | **Cured on live `b314` install** (nested migrations gone; journal identity OK) | Cursor (maintain) | Live journal↔source byte-equal; no nested `migrations/migrations`; startup clean. |
| **SPEC-2** (durable Git candidate) | **Partial** — overnight branch + workflows on `tryveto`; Mac base/spec commits still missing | Sebastian + Cursor | Candidate + product-base + spec-commit all resolvable on durable remote. |
| **SPEC-3** (ship immutability history) | **Historical not cured** — prior void/reclose remains; Section-2 corrective used archive-supersede going forward | Cursor (honest disposition only) | Disposition says historical SPEC-3 uncured; no rewrite of closed history; new ships append-only. |
| **Thinking-fast / Luna fast-decision** | **Broken on live** — `fast-decision.js` **absent** from `b314` release; agent `error`, model currently `cursor` (router fallthrough); `fast_decision_only` alias invalid | Cursor (restore into source + prove) | Canaries: no-work ≪1s `no_op`; light ~2–5s Luna; escalate → Thinking-slow; zero `Invalid route alias "fast_decision_only"`. |
| **Inkbox integration** | **Degraded** — `paperclip.inkboxwire.com` → `:8791` receiver up; root/webhook paths 404 on naive probe; **Omniagent paused** + model `gpt-5.6-luna` (router reject risk); **VET-409** `backlog` assignee Thinking-slow | Cursor (receiver/ops) + **Sebastian** (assignee taste) | Owner iMessage + email dual-webhook round-trip; no 500 on valid ingress; display-name phone quirk documented or fixed; VET-409 assignee matches chosen DM owner; Omniagent unpaused on allowed route (`cursor`→Auto) **or** intentional paused + alternate path proven. |
| **Desktop UX — View run** | **Patch in worktree** — `IssueChatThread` drops `target=_blank` (Electron no-op); not yet cut over to live | Cursor | Click View run in Desktop navigates in-app to run; cut over with next live install after gate or as reversible UI-only prep if Sebastian wants early. |
| **Section 2 / natural-run / instruction stack** | Section 2 corrective **closed on live `b314`**; natural-run observation + instruction collapse still open for “usable company” | Cursor | First-20 natural-run observation after clean recovery (canary/probe excluded); hire path → `paperclip-create-agent`; bundled instructions validated; STD contradictions dispositioned or fixed under next version. |

## Explicit asks for Sebastian

1. **Mac pin push (P0 for v0.07 close)** — from the Mac checkout that has both objects:
   ```sh
   git push tryveto 9cad4cb71670c00191e52ab44e877156dfaf2118:refs/tags/veto/v0.06-product-base-9cad4cb
   git push tryveto 11f920182fdf908e2476c144505b1e659427015d:refs/tags/veto/v0.07-spec-11f92018
   ```
   Then tell Cursor to fetch + re-run live-safe audit. Do **not** accept blob-only substitutes.

2. **Inkbox DM assignee** — VET-409 is on Thinking-slow today while Omniagent (historical DM workhorse) is **paused**. Choose one:
   - **A)** Omniagent owns owner DM (unpause + fix router model to `cursor` / Auto) — VET-409 → Omniagent
   - **B)** Thinking-fast owns light DM via restored fast-decision; Thinking-slow only on escalate
   - **C)** Keep Thinking-slow on VET-409 (current) and accept latency / cost for owner chat

3. **Desktop early cutover?** — Ship View-run patch on live before vs after v0.07 close (UI-only; does not unlock next version).

4. **Product taste for “1.0 bar”** — Confirm the one-sentence outcome above, or tighten (e.g. “Inkbox phone canary required” vs “board-only OK for 1.0”).

5. **Version key name** — see recommendation below (one decision at unlock time).

## Version naming recommendation

**Prefer Paperclip version key `v1.0` once v0.07 closes — do not open `v0.08`.**

| Option | Verdict |
|---|---|
| **`v1.0` (recommended)** | Matches Sebastian’s product goal; retires endless 0.0x theater; at unlock rename/retarget VET-582 from “v0.08 dry-run” → `v1.0` root. Fold prior “Three Front Doors” / Inkbox Desktop intent into the v1.0 plan doc. |
| `v0.08` | Compatible with existing VET-582 title and archival v0.08 draft, but understates the milestone and invites another 0.0x after. |
| Both | **Forbidden** — one next version only. |

Until then: VET-582 stays `backlog`; no DB unlock; prep work lands on the overnight closeout branch / ops without claiming a new version.

## Non-actions (this frame)

- Did **not** open or unlock v0.08 / v1.0 in product or DB.
- Did **not** claim v0.07 safe-close or audit PASS.
- Did **not** cut over live for fast-decision or Desktop (prep/frame only).
- tryveto.com prod (`sites/tryveto-static` → `veto-website`) untouched.

## Immediate next actions

| Who | Do |
|---|---|
| Sebastian | Push Mac pins (ask #1); pick Inkbox assignee (ask #2); confirm v1.0 key (ask #5). |
| Cursor (parallel, reversible) | Restore `fast-decision` into worktree from known-good lineage; keep Desktop View-run patch ready; Inkbox receiver 500 repro on `:8791` without unpausing Omniagent until ask #2. |
| Cursor (after pins) | Fetch → `audit-v007-live-safe` → honest PASS receipt → unlock `v1.0` only then. |
